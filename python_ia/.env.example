PORT=5000

# ── Base de datos (misma MySQL que el backend) ──
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=railway
# SSL solo para Aiven / TiDB (Railway = false)
DB_SSL=false
# DB_SSL_CA_PATH=/etc/secrets/ca.pem
